mutation CreatePost($text: String!, $newFileName: String) {
    createPost(text: $text, newFileName: $newFileName) {
        post {
            ...PostResult
        }

        errors {
            ...RegularError
        }
    }
}
